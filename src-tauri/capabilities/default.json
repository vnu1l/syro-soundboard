{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Local permissions for the Syro desktop window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
